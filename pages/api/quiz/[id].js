import connectDB from '../../../db/connect';
import {Quiz, Category, Score} from '../../../db/Quiz';

export default function edit(){
    const {id} = req.query;
    if(req.method === 'EDIT'){
        
    }
}

const handler = async(req,res) => {
    const {id} = req.query;
    c
    if(req.method === 'EDIT'){

    }
    if(req.method === 'POST'){

    }
}

export default connectDB(handler);